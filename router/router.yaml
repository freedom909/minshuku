# router.yaml

supergraph:
  listen: 0.0.0.0:4000

  subgraphs:
    users:
      routing_url: http://localhost:4001
    accounts:
      routing_url: http://localhost:4002
    profiles:
      routing_url: http://localhost:4003
    listings:
      routing_url: http://localhost:4004
    bookings:
      routing_url: http://localhost:4005
    carts:
      routing_url: http://localhost:4006
    payments:
      routing_url: http://localhost:4007
    reviews:
      routing_url: http://localhost:4008

# Define additional settings or plugins if needed
plugins:
  - name: apollo
    config:
      key: "service:minshuku-users:EkVfpI-4wFTOV0B4YyGfsQ"
      graph: "flyToWashington-mc4a9o@current"
