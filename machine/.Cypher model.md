// Guests
CREATE (:Guest {id: "g1", name: "Alice"});
CREATE (:Guest {id: "g2", name: "Bob"});

// Hosts
CREATE (:Host {id: "h1", name: "Host One"});
CREATE (:Host {id: "h2", name: "Host Two"});

// Listings
CREATE (:Listing {id: "l1", title: "Seaside Apartment"});
CREATE (:Listing {id: "l2", title: "Mountain Cabin"});

// Bookings
CREATE (:Booking {id: "b1", date: "2025-09-01"});
CREATE (:Booking {id: "b2", date: "2025-09-15"});

// Reviews
CREATE (:Review {id: "r1", text: "Great stay!", rating: 5});
CREATE (:Review {id: "r2", text: "Too noisy.", rating: 2});

// Sentiment Nodes (分析用)
CREATE (:Sentiment {id: "s1", label: "Positive"});
CREATE (:Sentiment {id: "s2", label: "Negative"});

// Trend Nodes
CREATE (:Trend {id: "t1", name: "Beach Holiday"});
CREATE (:Trend {id: "t2", name: "Winter Skiing"});

// Promotion Nodes
CREATE (:Promotion {id: "p1", type: "Discount", value: "10%"});
CREATE (:Promotion {id: "p2", type: "Special Offer", value: "Stay 3 nights, pay 2"});
