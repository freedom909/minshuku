客户服务（Customer Service）

Guest → Review → Listing → Host

支撑评论生成、回复、房东管理

数据分析（Analytics）

Listing ↔ Booking ↔ Guest

Review → Sentiment

Listing → Trend

支撑预订趋势、入住率、评论情感分析

推荐系统（Recommendation）

Guest → RecommendedListing

Listing → SimilarListing

Listing → Promotion

支撑个性化推荐、相似房源推荐、动态定价与促销

____________________________________
flowchart LR
    A[前端请求] --> B[AI 工作手册指令]
    B --> C[AI 服务层]
    C --> D[调用标题优化 API]
    C --> E[调用描述改进 API]
    C --> F[调用评论回复 API]
    D --> G[返回前端 & 更新 DB]
    E --> G
    F --> G
    G --> H[日志记录]
